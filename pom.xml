<?xml version="1.0" encoding="UTF-8"?>
<!--
  Sunrise Dental Clinic - CIS6003 WRIT1

  ZERO-FRAMEWORK BUILD. Exactly two external artifacts are declared, and neither
  is an application framework:

    1. mysql-connector-j  - a JDBC *driver* (implements java.sql.Driver). Without
                            it the JVM cannot open a MySQL socket at all. It
                            imposes no architecture on the application.
    2. junit-jupiter      - TEST SCOPE ONLY, never on the runtime classpath.
                            Task C of the brief explicitly requires test classes
                            and test automation, so a runner is mandated.

  Everything else - the HTTP server, the DI container, routing, filters, JSON,
  sessions, cookies, password hashing and the connection pool - is written by
  hand against the JDK. See my-docs/PROJECT-PLAN.md section 1.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>lk.icbt</groupId>
  <artifactId>sunrise-clinic</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>Sunrise Dental Clinic</name>
  <description>Appointment and patient management system - plain Java, no frameworks</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <main.class>lk.icbt.dentalclinic.Main</main.class>
    <mysql.version>9.5.0</mysql.version>
    <junit.version>5.11.4</junit.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>${mysql.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>sunrise-clinic</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>21</release>
          <compilerArgs>
            <arg>-Xlint:all</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <includes>
            <include>**/*Test.java</include>
            <!-- Integration tests need MySQL; they skip themselves when it is absent. -->
            <include>**/*IT.java</include>
          </includes>
        </configuration>
      </plugin>

      <!-- Copy the JDBC driver next to the jar so `java -jar` works without a fat jar. -->
      <!--
        JaCoCo: coverage measurement for Task C. Build tooling, not an application
        dependency - it never reaches the runtime classpath.
      -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.8.1</version>
        <executions>
          <execution>
            <id>copy-runtime-dependencies</id>
            <phase>package</phase>
            <goals><goal>copy-dependencies</goal></goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
              <includeScope>runtime</includeScope>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>${main.class}</mainClass>
              <addClasspath>true</addClasspath>
              <classpathPrefix>lib/</classpathPrefix>
            </manifest>
          </archive>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
